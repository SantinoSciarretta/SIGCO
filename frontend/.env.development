# Variables de entorno para el servidor de desarrollo de Vite.
#
# Este archivo SI se versiona: no contiene credenciales, solo la direccion del
# backend en la maquina local. Vite unicamente expone al navegador las
# variables que empiezan con VITE_.

VITE_API_URL=http://localhost:8080/api
