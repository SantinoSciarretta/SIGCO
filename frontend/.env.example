# Variables de entorno del frontend.
#
# En desarrollo se usa .env.development, ya versionado.
# En produccion, esta misma variable se define en el panel de Vercel apuntando
# al backend publicado en Railway:
#
#   VITE_API_URL=https://<backend-en-railway>/api

VITE_API_URL=http://localhost:8080/api
